apiVersion: v1
kind: Pod
metadata:
  name: curl-shell
  namespace: default
spec:
  serviceAccountName: listallresources
  containers:
    - name: curl
      image: curlimages/curl
      command: ["sh", "-c", "sleep 300"]
      stdin: true
      tty: true
  restartPolicy: Never
